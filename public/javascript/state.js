class State{
  constructor(){
      this.image_ids = {},
      this.image_id = 1,
      this.available_folders = [],
      this.available_tags = [],
      favorites: new Set()
  }

  update(newState){
    keys = Object.keys(newState);

    
  }


}
